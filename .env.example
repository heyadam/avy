# Development mode - set to "true" to use env vars instead of user-provided keys
# In production, omit this or set to "false" to require users to provide their own keys
NEXT_PUBLIC_DEV_MODE=true

# AI Provider API Keys (used when DEV_MODE is true)
OPENAI_API_KEY=your_openai_api_key_here
GOOGLE_GENERATIVE_AI_API_KEY=your_google_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
